{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "prepare": "lerna run prepare"
  },
  "devDependencies": {
    "lerna": "^2.10.0",
    "rimraf": "^2.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
