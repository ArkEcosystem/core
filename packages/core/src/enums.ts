export enum ProcessState {
    Errored = "errored",
    Launching = "launching",
    OneLaunch = "one-launch-status",
    Online = "online",
    Stopped = "stopped",
    Stopping = "stopping",
    WaitingRestarting = "waiting restart",
}
