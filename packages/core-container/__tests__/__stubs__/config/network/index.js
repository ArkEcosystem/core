const dynamicFees = require("./dynamic-fees.json");
const milestones = require("./milestones.json");
const network = require("./network.json");

module.exports = { dynamicFees, milestones, network };
