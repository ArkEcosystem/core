export declare const defaults: {
    initialization: {
        capSQL: boolean;
        promiseLib: any;
        noLocking: boolean;
    };
    connection: {
        host: string;
        port: string | number;
        database: string;
        user: string;
        password: string;
    };
    estimateTotalCount: boolean;
};
