export const walletRepository = {
    // tslint:disable-next-line:no-empty
    findByPublicKey: publicKey => {},
    // tslint:disable-next-line:no-empty
    index: () => {},
    getIndex: () => ({ values: () => [] }),
};
