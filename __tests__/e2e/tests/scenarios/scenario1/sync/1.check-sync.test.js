"use strict";

const testUtils = require("../../../../lib/utils/test-utils");

describe("Check node0 has peers", () => {
    it("to be done", async () => {
        expect(true).toBe(true);
    });

});
