name: Unit Testing

on:
    push:
        branches:
            - "master"
            - "develop"
    pull_request:
        types: [ready_for_review, synchronize, opened]

jobs:
    core:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage --coverageDirectory .coverage/unit/core

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core/clover.xml
                  fail_ci_if_error: true

    core-api:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-api --coverageDirectory .coverage/unit/core-api

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-api/clover.xml
                  fail_ci_if_error: true

    core-blockchain:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-blockchain --coverageDirectory .coverage/unit/core-blockchain

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-blockchain/clover.xml
                  fail_ci_if_error: true

    core-cli:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-cli --coverageDirectory .coverage/unit/core-cli

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-cli/clover.xml
                  fail_ci_if_error: true

    core-database:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-database --coverageDirectory .coverage/unit/core-database

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-database/clover.xml
                  fail_ci_if_error: true

    core-forger:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-forger --coverageDirectory .coverage/unit/core-forger

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-forger/clover.xml
                  fail_ci_if_error: true

    core-kernel:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-kernel --coverageDirectory .coverage/unit/core-kernel

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-kernel/clover.xml
                  fail_ci_if_error: true

    core-logger-pino:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-logger-pino --coverageDirectory .coverage/unit/core-logger-pino

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-logger-pino/clover.xml
                  fail_ci_if_error: true

    core-magistrate-api:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-magistrate-api --coverageDirectory .coverage/unit/core-magistrate-api

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-magistrate-api/clover.xml
                  fail_ci_if_error: true

    core-magistrate-crypto:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-magistrate-crypto --coverageDirectory .coverage/unit/core-magistrate-crypto

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-magistrate-crypto/clover.xml
                  fail_ci_if_error: true

    core-magistrate-transactions:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-magistrate-transactions --coverageDirectory .coverage/unit/core-magistrate-transactions

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-magistrate-transactions/clover.xml
                  fail_ci_if_error: true

    core-manager:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-manager --coverageDirectory .coverage/unit/core-manager

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-manager/clover.xml
                  fail_ci_if_error: true

    core-p2p:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-p2p --coverageDirectory .coverage/unit/core-p2p

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-p2p/clover.xml
                  fail_ci_if_error: true

    core-snapshots:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-snapshots --coverageDirectory .coverage/unit/core-snapshots

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-snapshots/clover.xml
                  fail_ci_if_error: true

    core-state:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-state --coverageDirectory .coverage/unit/core-state

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-state/clover.xml
                  fail_ci_if_error: true

    core-test-framework:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-test-framework --coverageDirectory .coverage/unit/core-test-framework

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-test-framework/clover.xml
                  fail_ci_if_error: true

    core-transaction-pool:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-transaction-pool --coverageDirectory .coverage/unit/core-transaction-pool

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-transaction-pool/clover.xml
                  fail_ci_if_error: true

    core-transactions:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-transactions --coverageDirectory .coverage/unit/core-transactions

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-transactions/clover.xml
                  fail_ci_if_error: true

    core-webhooks:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/core --coverage-webhooks --coverageDirectory .coverage/unit/core-webhooks

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/core-webhooks/clover.xml
                  fail_ci_if_error: true

    crypto:
        runs-on: ubuntu-latest

        strategy:
            matrix:
                node-version: [12.x]

        steps:
            - uses: actions/checkout@v1

            - name: Cache node modules
              uses: actions/cache@v1
              with:
                  path: node_modules
                  key: ${{ runner.os }}-node-${{ hashFiles('**/yarn.lock') }}
                  restore-keys: ${{ runner.os }}-node-

            - name: Use Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v1
              with:
                  node-version: ${{ matrix.node-version }}

            - name: Install & Build
              run: yarn setup

            - name: Test
              run: yarn test:parallel __tests__/unit/crypto --coverage --coverageDirectory .coverage/unit/crypto

            - name: Codecov
              uses: codecov/codecov-action@v1
              with:
                  file: .coverage/unit/crypto/clover.xml
                  fail_ci_if_error: true
