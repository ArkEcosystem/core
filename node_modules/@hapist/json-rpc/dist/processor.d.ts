import { Server } from "@hapi/hapi";
import { IProcessorOptions, IRequestParameters, IResponse, IResponseError } from "./interfaces";
export declare class Processor {
    private readonly server;
    private readonly options;
    constructor(server: Server, options: IProcessorOptions);
    resource<T = any>(payload: IRequestParameters): Promise<IResponse<T> | IResponseError>;
    collection<T = any>(payloads: IRequestParameters[]): Promise<Array<IResponse<T>> | IResponseError[]>;
    private createSuccessResponse;
    private createErrorResponse;
}
