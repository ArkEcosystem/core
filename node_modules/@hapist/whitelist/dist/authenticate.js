"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const remote_address_1 = require("@faustbrian/remote-address");
const boom_1 = __importDefault(require("@hapi/boom"));
const mm = __importStar(require("micromatch"));
const config_1 = require("./config");
exports.authenticate = (request, h) => {
    const credentials = config_1.config.get("trustHeaders")
        ? remote_address_1.discover(request, config_1.config.get("headers"))
        : request.info.remoteAddress;
    if (!credentials) {
        return h.continue;
    }
    for (const ip of config_1.config.get("whitelist")) {
        if (mm.isMatch(credentials, ip)) {
            return config_1.config.get("strategy") === "auth"
                ? h.authenticated({ credentials: credentials })
                : h.continue;
        }
    }
    return boom_1.default.unauthorized();
};
//# sourceMappingURL=authenticate.js.map