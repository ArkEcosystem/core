/**
 * Globber interface.
 */
export interface IGlobber {
    /**
     * Globs for package.json files.
     *
     * @param pattern
     */
    globPackageFiles(pattern: string): Promise<Array<string>>;
}
/**
 * Creates a globber.
 */
export declare function createGlobber(): {
    globPackageFiles(pattern: string): Promise<string[]>;
};
