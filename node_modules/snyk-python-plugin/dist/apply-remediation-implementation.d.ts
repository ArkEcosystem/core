import { ManifestFiles, DependencyUpdates } from './types';
import { legacyCommon } from '@snyk/cli-interface';
export declare function applyUpgrades(manifests: ManifestFiles, upgrades: DependencyUpdates, topLevelDeps: legacyCommon.DepTree): void;
